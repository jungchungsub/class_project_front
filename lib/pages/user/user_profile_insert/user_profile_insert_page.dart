import 'package:flutter/material.dart';

class UserProfileInsertPage extends StatelessWidget {
  const UserProfileInsertPage({super.key});

  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text("UserProfileInsertPage"),
    );
  }
}
